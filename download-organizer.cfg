// Audio
audio={flac, mp3, aac, alac, m4a, wav}

// Video
video={mp4, mov, mkv, webm}

// Photos
photos={gif, bmp, jpeg, jpg, png, avif}

// Executables
runnable={dmg, deb, rpm, exe}

// Compressed / Archived files
compressed={tgz, zst, tar.zst, tar.gz, gz, 7z, rar, zip}

// Code
code={js, ts, py, c, c++, cpp, cc, java, go, rs, rb, sql}

// Documents
documents={txt, docx, pdf, odt, xlsx, xls, pptx}

// Misc - bootable images, and other stuff
misc={iso, qcow}


